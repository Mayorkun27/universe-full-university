{
     "version": 2,
     "builds": [
       { "src": "server.js", "use": "@vercel/node" }
     ],
     "routes": [
       { "src": "/api/v1/auth/(.*)", "dest": "/server.js" },
       { "src": "/api/v1/faculty/(.*)", "dest": "/server.js" }
     ]
   }   